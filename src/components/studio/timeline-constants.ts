/**
 * Width of the timeline's left gutter (the per-track visibility toggles).
 * The ruler, section headers and track rows all reserve the same width so the
 * lanes underneath line up with the ruler ticks.
 */
export const TIMELINE_GUTTER = "w-8";
