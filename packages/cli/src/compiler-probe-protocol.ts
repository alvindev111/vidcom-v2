/** Hidden argument that selects the separately killable compiler child. */
export const COMPILER_PROBE_SENTINEL = "--vidcom-compiler-probe";

/** Exact stdout contract emitted only after a real HyperFrames transform. */
export const COMPILER_PROBE_SUCCESS = "VIDCOM_COMPILER_PROBE_OK";

