// Generated by packages/agent-kit/scripts/build.mjs. Do not edit.
export const AGENT_KIT_VERSION = 9 as const;
export const AGENT_KIT_FILES = {
  "AGENTS.md": {
    "content": "<!-- x-vidcom-agent-kit: 9 -->\n# VidCom workspace instructions\n\n## Skills — use these first\n\nStart with `/vidcom`. It routes the request to one focused skill:\n\n| Intent | Skill |\n| --- | --- |\n| Create, adopt, or orient a project | `/vidcom-project` |\n| Add or change scenes, timing, or text | `/vidcom-scene` |\n| Animate a scene, add 3D, particles, or Lottie | `/vidcom-motion` |\n| Change tone, palette, subtitles, or BGM | `/vidcom-look` |\n| Author narration or generate TTS | `/vidcom-narration` |\n| Snapshot, render, or inspect a job | `/vidcom-render` |\n| Diagnose and repair a project | `/vidcom-fix` |\n\n## Standard workflow\n\n1. BOOTSTRAP by attempting `install_agent_kit` once for the active host, then read the installed main or `.vidcom.md` auxiliary instructions and `/vidcom` before choosing a workflow. Do not block on `manual_merge` or loop until ready.\n2. DISCOVER the server protocol, capabilities, and tools.\n3. ORIENT with `list_projects` and `get_project_context`. Never guess IDs or timing.\n4. STORYBOARD before editing. State `This video tells [audience] that [message]`, then give every beat a narrative role, viewer experience, primary story pattern, meaningful visual change, setup/development/payoff, and a concrete seam token. The value claim lands by beat two. In every rolling four story scenes use at least three primary patterns.\n5. EDIT + CHOREOGRAPH with VidCom tools and the latest hash or revision precondition. Mark agent-authored projects with `vidcomAgentKitVersion: 9` in `hyperframes.json`. Every story scene is 6–10 seconds, needs setup, development, payoff, and hold across its first, middle, and final thirds, and routes through `/vidcom-motion` before rendering.\n6. STYLE from explicit user or brand colors when present. Otherwise call `list_color_palettes` with a controlled mood when possible; compare `chooseWhen`, `avoidWhen`, energy, temperature, harmony, and recommended uses, then apply the best id through `set_preview_settings`. Use `clean-slate` when no stronger match is justified.\n7. SCORE after duration is known: call `search_bgm` with the intended mood, verify the chosen source and attribution, then pass that exact result to `install_bgm`. If remote catalogues are unavailable or unsuitable, use `list_bgm_beds` as the offline fallback. Background music is the default; omit it only when the user asks for no music or silence is editorially required.\n8. VALIDATE with `validate_project`; resolve every error before completion.\n9. PREVIEW with `start_snapshot` and inspect real frames.\n10. NARRATE with `start_tts` for story scenes; generated, current narration must cover at least 75% of story time.\n11. RENDER with `start_render`; poll `get_job_status` with backoff.\n12. REPORT changes, final revision, and every remaining diagnostic.\n\nOn `write_conflict`, re-read and merge. Never remove a precondition. Destructive work requires explicit user confirmation.\n\n## Tool reference\n\n| Level | Tools |\n| --- | --- |\n| Read | `list_projects`, `list_catalog_items`, `get_project_context`, `list_scenes`, `read_composition`, `list_project_assets`, `get_narration_cues`, `list_tts_voices`, `list_color_palettes`, `search_bgm`, `list_bgm_beds`, `validate_project`, `get_job_status`, `get_render_output` |\n| Write | `create_project`, `adopt_project`, `rename_project`, `create_scene`, `set_scene_timing`, `set_element_position`, `set_text`, `save_file`, `set_preview_settings`, `replace_narration_cues`, `patch_narration_cue`, `install_motion_library`, `reorder_scenes`, `move_scenes`, `generate_captions`, `mount_asset`, `install_catalog_item`, `install_bgm`, `import_bgm`, `record_bgm_license` |\n| Job | `start_snapshot`, `start_tts`, `start_render`, `cancel_job` |\n| Destructive | `delete_file`, `delete_scene`, `delete_scenes`, `delete_project` |\n| Workspace | `install_agent_kit` |\n\nNo UI is required: `create_project` or `adopt_project` starts the work, `set_preview_settings` handles look and BGM, and `get_render_output` names the finished MP4 on disk. Only choosing the workspace directory and browsing the filesystem stay outside the tool surface.\n\n## Project structure\n\n- `index.html`: root composition and timeline mounts.\n- `compositions/`: one file per scene.\n- `assets/`: project-owned media; `assets/vendor/` holds pinned motion libraries.\n- `narration/`: narration cue sidecars and generated audio.\n- `preview-settings.json`: tone, subtitles, BGM, and preview settings.\n- `renders/`: VidCom-produced video artifacts.\n\n## Key rules\n\n- Give timed elements `class=\"clip\"`, `data-start`, `data-duration`, and `data-track-index`.\n- Keep GSAP timelines paused and register them at `window.__timelines[compositionId]`.\n- Add a motion library with `install_motion_library` and reference the vendored path it returns. Never load one from a CDN: the render stops being reproducible and resolves nothing offline.\n- For Vietnamese, Japanese, Korean, Chinese, or any other non-ASCII text, use a project-local `@font-face`. `validate_project` reads the actual font bytes and checks every authored Unicode code point; do not trust `system-ui`, a named machine font, or a remote font to render consistently.\n- Put every new scene in a separate sub-composition file mounted with `data-composition-src`.\n- On every story-scene root author `data-scene-role=\"story\"`, a content-specific `data-story-pattern`, and—after the first scene—`data-seam-kind=\"carry|transform|contrast\"` plus a concrete `data-seam-token`. Never use `fade`, `slide`, or `cards` as a primary pattern.\n- Story-driven video is the default. Build a value-first spine: hook in the viewer's language, tension or evidence that develops the claim, and a payoff that resolves it.\n- A scene is a story beat, not a slide. Its motion must reveal information, transform visual state, demonstrate cause/effect, move the camera to a new idea, or physically hand off to the next beat.\n- Give every non-trivial story scene multi-phase choreography: setup → development → payoff → hold. Compose 2-4 motion patterns and vary motion verbs, directions, and eases across scenes.\n- Distribute verified actions across the first, middle, and final thirds. Over every rolling four story scenes, keep at least three primary patterns; repeated adjacent patterns need a matching carry/transform token and a visible state handoff.\n- A lone fade, gentle rise/drop, or repeated `opacity + y` entrance is transition polish, not scene motion. Never use it as the primary choreography or repeat one entrance recipe across the video.\n- A tween after its scene clip ends never runs; move it or extend the clip.\n- Move a selected authored element or caption group with `set_element_position`; use the exact `data-hf-id` and current source hash, never a selector or client-supplied source path.\n- End on a held frame: the last scene runs 1-3s past its narration, every other scene about 0.3s. A video that stops on the final consonant feels cut off, not finished.\n- Change colors, tone, subtitle styling, and BGM through `set_preview_settings`, not composition source. Explicit user or brand colors always win; otherwise choose from the source-backed `list_color_palettes` guidance and apply `theme.paletteId`. An individual color override intentionally clears the preset id.\n- Background music is the default for every video unless the user requests no music or silence is editorially required. Add it after composition duration is known.\n- A file a person copied into the project never announces itself: run `list_project_assets` to find it, then attach a track with `set_preview_settings`.\n- Prefer `search_bgm` for real mood-matched instrumental music. Inspect `sourceUrl`, `license`, and `attribution`; pass only the chosen `{providerId, trackId}` to `install_bgm`, which revalidates that exact track and freezes its bytes, licence, and provenance locally. Never invent clearance from a search result.\n- Remote music is fail-soft: if providers are empty, rate-limited, or unsuitable, `list_bgm_beds` shows five built-in beds and `install_bgm` renders one at the project's own length. `import_bgm` records a supplied track's licence — state `unknown` rather than guessing, and use `record_bgm_license` only after somebody establishes the answer.\n- Keep rendering deterministic: no `Date.now()`, `Math.random()`, or network fetch.\n- Do not edit project files with host file tools or run HyperFrames CLI beside VidCom. If raw source editing is explicitly requested, use `save_file` with `expectedContentHash`.\n\n## Validate — always\n\nRun `validate_project` after every edit. Fix all `error` diagnostics before reporting completion. Then snapshot and inspect the affected frames.\n\n## Troubleshooting\n\n- `write_conflict`: refresh project context and retry from the new hash/revision.\n- `platform-mismatch`: align root width, height, and FPS with project platform settings.\n- `narration-overflow`: shorten/re-time the cue or extend the scene within duration limits.\n- `missing-asset`: restore or replace the referenced project-relative asset.\n- `story-motion-shallow`: redesign the beat with a meaningful state change across at least two statically visible phases; fade/position polish does not count.\n- `story-motion-unverified`: replace dynamic selectors with resolvable targets or add meaningful, statically verifiable GSAP choreography before render.\n- `story-metadata-missing`, `story-seam-missing`: add the v9 scene role, pattern, and concrete seam metadata.\n- `story-pattern-repeated`, `story-pattern-diversity`: redesign the sequence so a rolling four uses at least three primary patterns and any continuation has a visible matching handoff.\n- `story-scene-duration`: retime generated story scenes to 6–10 seconds.\n- `story-scene-static-too-long`: add current narration and continuing middle/final state change, or split the beat into shorter scenes.\n- `story-narration-sparse`: generate current narration until its clamped windows cover at least 75% of story time.\n- `remote-motion-library`: run `install_motion_library` and swap the CDN tag for the returned vendored tag.\n- `text-encoding-invalid`: re-save the referenced source as UTF-8 without replacement bytes.\n- `font-file-invalid`: restore or replace the referenced local OpenType font file.\n- `font-glyph-missing`: choose a local font whose `cmap` covers every code point listed in `details.missingCodePoints`.\n- `font-coverage-unverified`: vendor the selected font into the project and bind it with `@font-face`; machine or remote fallback is not portable evidence.\n- `lint:*`: apply the named HyperFrames rule, then validate again.\n",
    "contentHash": "sha256:0d140967ce202b56fa6ecd9e4278cffaabed3aa15984eea1602049ef86299e73"
  },
  "skills/vidcom/SKILL.md": {
    "content": "---\nname: vidcom\ndescription: Route complete video-production requests through VidCom MCP. Use first for any VidCom project, scene, look, narration, validation, preview, or render task. Delegate focused work to a vidcom-* skill; do not use host file tools or HyperFrames CLI for project mutations.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom router\n\n1. Attempt `install_agent_kit` once for the active host, then read the installed main or `.vidcom.md` auxiliary instructions and this router before selecting any other video skill. If the host is degraded because its main file is foreign, proceed from the auxiliary file; do not block on manual merge or retry in a loop.\n2. Discover VidCom MCP tools.\n3. Run `list_projects`, then `get_project_context` for the selected project.\n4. Before edits, state `This video tells [audience] that [message]` and propose a value-first beat table. Each row names its narrative role, viewer experience, primary story pattern, meaningful visual change, setup/development/payoff, and a concrete seam token. The value claim lands by beat two; every rolling four story scenes use at least three primary patterns.\n5. Route project setup to `/vidcom-project`, story beats to `/vidcom-scene`, every story scene's choreography to `/vidcom-motion`, look changes to `/vidcom-look`, speech to `/vidcom-narration`, output to `/vidcom-render`, and failures to `/vidcom-fix`.\n6. Route styling through `/vidcom-look`: explicit user or brand colors win; otherwise select a standardized palette from `list_color_palettes` and apply its id.\n7. Add BGM through `/vidcom-look` after duration is known: search remote catalogues by mood first, verify the selected source and attribution, and use an offline bed when providers are unavailable. Skip music only when the user asks for no music or silence is editorially required.\n8. Always finish with validation, a real snapshot, and a report of remaining diagnostics.\n\nDo not use this router as a substitute for a focused skill once the intent is known. A complete story-driven video must pass through both `/vidcom-scene` and `/vidcom-motion`; a fade-only or repeated opacity-plus-translate treatment is not a finished scene.\n\nExample: `install_agent_kit` → read `/vidcom` → `list_projects` → `get_project_context` → `/vidcom-scene` → `/vidcom-motion` → `/vidcom-look` → `/vidcom-fix` → `/vidcom-render`.\n",
    "contentHash": "sha256:b41679e161d5fd23986e1e9b15b66ed6beeebc6335134823bcdca56df945d322"
  },
  "skills/vidcom-project/SKILL.md": {
    "content": "---\nname: vidcom-project\ndescription: Create, adopt, list, or orient VidCom projects through MCP. Use when selecting a workspace project or establishing project structure. Do not use for scene content, visual styling, narration, or rendering.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom project\n\nRead `list_projects` before choosing a `projectId`, then call `get_project_context`. Preserve the returned identity, platform, revision, and paths. Ask before adopting, renaming, or deleting anything.\n\nDo not edit `vidcom.json`, `index.html`, or workspace instructions with host file tools.\n\nExample: `list_projects` → choose with the user → `get_project_context` → report platform, scenes, revision, and diagnostics.\n",
    "contentHash": "sha256:4fea4bd1baa0dc2477117d7986d3e6345f54c4cee7ed2941f47737249d2b91e2"
  },
  "skills/vidcom-scene/SKILL.md": {
    "content": "---\nname: vidcom-scene\ndescription: Create story beats and change scene timing or text through VidCom MCP. Use for a video's value-first narrative spine, beat ordering, tracks, duration, and script edits. Do not use for motion choreography, tone, subtitles, BGM, narration generation, or rendering.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom scene\n\nStart with `get_project_context`; never guess `sceneId`, track, timing, element identity, or hashes. Use `create_scene`, `set_scene_timing`, `set_element_position`, or `set_text` with the latest `expectedContentHash`. `set_element_position` accepts only the exact authored `data-hf-id` returned by the current scene projection; it never accepts a selector or source path. A script edit may stale its matching narration cue. On conflict, re-read and merge.\n\nBefore creating scenes, state `This video tells [audience] that [message]`. Build a value-first spine: hook in outcome language, land the value claim by beat two, develop it through tension/evidence, then resolve it with a payoff. For each beat specify its narrative role, what the viewer experiences, a content-specific primary pattern, the meaningful visual change, setup/development/payoff, and its concrete handoff to the next beat. Cut any beat whose role cannot be traced to the message.\n\nMark agent-authored projects with `vidcomAgentKitVersion: 9` in `hyperframes.json`. Time each generated story scene to 6–10 seconds. Author `data-scene-role=\"story\"` and `data-story-pattern` on every story root; after the first scene add `data-seam-kind=\"carry|transform|contrast\"` plus a concrete `data-seam-token` naming the object, shape, color, direction, or question passed across the cut. Plan at least three primary patterns in every rolling four scenes. `fade`, `slide`, and `cards` are not primary pattern names.\n\nAfter scene structure and text are in place, route every story scene through `/vidcom-motion`. A scene is not complete merely because its text fades or slides into view.\n\nDo not create inline scene hosts or edit composition files with host tools. Use `save_file` only when the user explicitly requests raw source editing.\n\nTime the last scene to outlive its narration by 1-3s. Cutting the video on the final consonant reads as a dropped call rather than an ending, so hold the closing frame — its title, mark, or credit line — while the music decays. Every other scene needs only its own breath (about 0.3s past its cue). When background music was sized to the old duration, re-install the bed after the change so its fade still lands on the last frame.\n\nExample: `get_project_context` → propose the value-first beat table → `create_scene` at the intended index/track → `set_text` → `/vidcom-motion` → `validate_project` → `start_snapshot`.\n",
    "contentHash": "sha256:28bc7e798da59731bbb645dfbd0b02e358211529127d3d0db020e3ae37612057"
  },
  "skills/vidcom-look/SKILL.md": {
    "content": "---\nname: vidcom-look\ndescription: Adjust VidCom tone, palette, subtitle presentation, transitions, and BGM through preview settings. Use for visual or audio-bed direction. Do not use for scene structure, narration speech, diagnostics repair, or final rendering.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom look\n\nRead `get_project_context` and current preview settings first. Apply look changes through the available preview-settings tool with the latest revision. Keep scene source unchanged for tone, subtitle, and BGM changes.\n\nHonor explicit colors from the user or brand kit before any preset. When neither provides color direction, call `list_color_palettes` with a controlled `mood` when the brief supports one. Compare `guidance.chooseWhen`, `avoidWhen`, `energy`, `temperature`, `harmony`, and `recommendedFor`; use `source.url` and `source.swatches` as provenance, not as a popularity guarantee. Then call `set_preview_settings` with `patch.theme.paletteId`. Use the returned `defaultPaletteId` only when no catalog entry has a better rationale. Do not copy the swatches by hand: selecting the id atomically maps the sourced colors to primary, accent, background, surface, text, tone lights, and subtitle colors. A later individual color override turns the look into a custom palette and clears the preset id.\n\nFor every video, add BGM by default once composition duration is known. Call `search_bgm` with the intended mood first. Compare instrumental tags, duration, source, licence, and attribution; verify the chosen source rather than treating discovery as publication clearance. Pass that exact `{providerId, trackId}` to `install_bgm`, which revalidates and freezes the bytes locally. If every provider is unavailable or no result fits, call `list_bgm_beds` and install an offline bed. Skip music only when the user explicitly requests no music or silence is editorially required.\n\nDo not use `save_file` to restyle a composition when preview settings cover the request.\n\nExample: `get_project_context` → honor explicit colors or `list_color_palettes` → `set_preview_settings` → `search_bgm` → verify/select, or `list_bgm_beds` as fallback → `install_bgm` → `validate_project` → `start_snapshot` → inspect the affected frames.\n",
    "contentHash": "sha256:2d7dd970fefb5054b42aafd9539023062137751f131a98e8a5b2695b9691627b"
  },
  "skills/vidcom-motion/SKILL.md": {
    "content": "---\nname: vidcom-motion\ndescription: Choreograph meaningful multi-phase motion for every VidCom story scene with GSAP or another vendored runtime. Use when motion must reveal meaning, transform visual state, demonstrate cause/effect, direct the camera, or hand off between beats. Do not use for scene timing and text, tone and BGM, narration, or rendering; fade-only entrances never satisfy this skill.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom motion\n\nRead `get_project_context` first. Pick one library, vendor it with `install_motion_library`, then write the timeline with `save_file` using the current `expectedContentHash`.\n\n## Story-motion contract\n\nMotion is the storytelling mechanism, not decoration. Before writing source, make a motion map for every beat:\n\n| Field | Required answer |\n| --- | --- |\n| Narrative role | Why this beat exists in the value-first story |\n| Viewer experience | The visual world and feeling, not a layout description |\n| Meaningful change | What information, state, relationship, cause/effect, or viewpoint changes |\n| Choreography | Setup → development → payoff → hold |\n| Motion verbs | A specific verb for every moving element |\n| Handoff | How motion carries attention into the next beat |\n\nWrite that plan into each story root: `data-scene-role=\"story\"`, a content-specific `data-story-pattern`, and after the first scene a `data-seam-kind` of `carry`, `transform`, or `contrast` plus a concrete `data-seam-token`. Never label the primary pattern `fade`, `slide`, or `cards`. Over every rolling four story scenes use at least three primary patterns; an adjacent repeat is valid only when both scenes share a visible carry/transform token.\n\nFor every non-trivial story scene:\n\n- Compose 2-4 complementary motion patterns on one paused timeline. At least one primary action must be spatial, structural, illustrative, data-driven, camera-driven, or a visible state transformation tied to the beat's meaning.\n- Use distinct phases across the first, middle, and final thirds: establish the visual world, develop or reveal the idea, land one clear payoff, then hold long enough to read. At least two phases need a statically verifiable scale, rotation, structural, or other meaningful state change. Overlap actions so the scene feels directed rather than sequentially faded.\n- Vary verbs, directions, depth, and eases across scenes. Every element gets a concrete verb such as draws, assembles, counts, tracks, pushes, morphs, or locks; “animates in” is not direction.\n- Treat fades, gentle rises/drops, and `opacity + y` as secondary transition polish only. A lone fade or repeated entrance recipe is shallow motion and must be redesigned before render.\n- Make setup, development, and payoff statically resolvable by VidCom. Dynamic selectors fail closed. Root `forEach` loops do not become evidence for every scene, and `data-no-timeline` never bypasses validation. The current gate reads parsed GSAP actions; when Lottie, Three.js, Anime.js, CSS, or WAAPI carries the primary effect, also choreograph a real GSAP-controlled container, camera, or state handoff across the three phases. It must reinforce the beat rather than act as a dummy validator bypass.\n\n## Pick the library\n\n| Need | Library | `libraryId` |\n| --- | --- | --- |\n| DOM and SVG motion, kinetic typography — the default | GSAP | `gsap` |\n| A lighter timeline when GSAP is more than the scene needs | Anime.js | `anime` |\n| Small browser-native motion over the Web Animations API | Motion One | `motion-one` |\n| Playing an After Effects animation delivered as Lottie JSON | Lottie | `lottie` |\n| 3D, shaders, particles | Three.js | `three` |\n\nPlain CSS animation and the Web Animations API need no library; use them only for secondary one-shot or ambient effects, not as the primary choreography of a story scene.\n\nDefault to GSAP and to one library per project. Two libraries animating the same element fight over its transform.\n\n## Never load a library from a CDN\n\nA remote `<script>` makes the render non-reproducible and resolves nothing once VidCom runs offline, which renders the scene motionless with no error. `install_motion_library` copies the pinned build into `assets/vendor/` and returns the exact `scriptTag` and `entry` path to reference — use them verbatim. A `remote-motion-library` diagnostic means a CDN tag is still in the source; replace it with the vendored tag.\n\nPaths in the tag stay project-relative on purpose. VidCom emits a `<base href>` for both preview and render, so a rewritten or absolute path is what breaks.\n\n## Global libraries versus a module library\n\nFour of the five install a browser global — load the returned `scriptTag` and call the global by name. Three.js has no global: its tag runs the module but binds nothing, so import the returned `importSpecifier` inside your own module script.\n\n```html\n<script type=\"module\">\n  import * as THREE from \"./assets/vendor/three-0.185.1/three.module.min.js\";\n  const scene = new THREE.Scene();\n</script>\n```\n\nIts two vendored files must stay side by side; the module resolves its sibling by name at load time.\n\nThe tool is idempotent: an \"already_installed\" status means the pinned copy is present and no revision was spent.\n\n## Write a seekable timeline\n\nVidCom renders by seeking to each frame's timestamp, so nothing may play on its own clock:\n\n- Create the timeline **paused** and register it as `window.__timelines[compositionId]`, where `compositionId` is the scene's `data-composition-id`.\n- Drive every value from timeline position only. No `Date.now()`, no `Math.random()`, no `requestAnimationFrame` loop, no autoplay, no network fetch.\n- For Three.js call `renderer.render(...)` from a timeline callback or after seeking, never from an animation loop.\n- A tween starting after the scene clip ends never runs. Extend `data-duration` or move the tween.\n\n## Example\n\n`get_project_context` → `install_motion_library` with `libraryId: \"gsap\"` → `save_file` on the scene source, adding the returned `scriptTag` and a paused timeline registered on `window.__timelines` → `validate_project` → `start_snapshot`, then inspect the frames to confirm the motion actually moved.\n",
    "contentHash": "sha256:88ce882a3e93e7c713da3a2301392e56c74aafe048f43e58ee8183927d1ddbf7"
  },
  "skills/vidcom-narration/SKILL.md": {
    "content": "---\nname: vidcom-narration\ndescription: Author narration cues, choose voices, and generate VidCom TTS jobs. Use when scenes contain spoken copy or captions tied to speech. Do not use for BGM, scene layout, silent previews, or final rendering.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom narration\n\nRead project context and scene scripts first. Use `list_tts_voices`, update the intended cue, then call `start_tts`. Treat its result as a job ID, not completed audio. Poll `get_job_status` with backoff until terminal.\n\nScript edits can stale one narration cue; regenerate only the stale cue when possible. Do not run a TTS CLI or write WAV files directly.\n\nExample: `get_project_context` → `list_tts_voices` → update cue → `start_tts` → backoff/poll `get_job_status` → `validate_project`.\n",
    "contentHash": "sha256:29c605ce50c2c3794d881507415559bec9fd02b578157ab4e467262ba5b91bef"
  },
  "skills/vidcom-render/SKILL.md": {
    "content": "---\nname: vidcom-render\ndescription: Create VidCom snapshots and renders and monitor their jobs. Use after edits are validated or when the user asks for preview frames or an exported video. Do not use to change scenes, styling, or narration content.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom render\n\nBefore validation, confirm the storyboard has a value-first story spine and each 6–10 second story scene source implements its motion map with setup, development, payoff, and hold across the first, middle, and final thirds. Verify at least three primary patterns in every rolling four, concrete seam tokens, and generated current narration over at least 75% of story time. Reject scenes whose primary choreography is only a fade, gentle rise/drop, or repeated `opacity + y`; route them through `/vidcom-motion` before rendering. Also confirm `get_project_context` reports an attached BGM track, unless the user requested no music or silence is editorially required.\n\nRun `validate_project` and stop on errors. For non-ASCII copy, require a project-local `@font-face`: `font-glyph-missing` lists unsupported code points, and `font-coverage-unverified` means a machine or remote fallback has no portable byte-level proof. `story-motion-shallow` and `story-motion-unverified` require `/vidcom-motion`; `story-pattern-diversity`, `story-pattern-repeated`, `story-seam-missing`, `story-scene-duration`, `story-scene-static-too-long`, and `story-narration-sparse` require sequence, timing, seam, or narration repair. `start_snapshot` and `start_render` enforce these gates, and `bestEffort` bypasses none. Poll snapshots with backoff, inspect every scene's payoff frame, then compare the contact sheet for pattern diversity and visible cross-scene handoffs—not merely visible text. Render only after that review. Report `partial`, warnings, cleanup status, and artifacts honestly.\n\nDo not run HyperFrames CLI beside VidCom or tight-loop job polling.\n\nExample: review story spine and motion map → read scene sources → `/vidcom-motion` for any shallow beat → `validate_project` → `start_snapshot` → poll/inspect payoffs → `start_render` → poll → report artifact and remaining diagnostics.\n",
    "contentHash": "sha256:1dbb9c394a08cc5ae0e261f658ae87ad3bb7e072f8259ca4a3406a8255b71b3a"
  },
  "skills/vidcom-fix/SKILL.md": {
    "content": "---\nname: vidcom-fix\ndescription: Diagnose and repair VidCom project errors and warnings through MCP. Use after validation fails, a job reports a project problem, or assets/timing/narration are inconsistent. Do not use for unrelated creative direction or unvalidated destructive cleanup.\nx-vidcom-agent-kit: 9\n---\n\n# VidCom fix\n\nCall `validate_project` and work from diagnostic codes, not message parsing. Read current project context and the referenced source before changing anything. Apply the smallest tool mutation with current preconditions, then validate again. Ask before destructive repair.\n\nFor `font-glyph-missing`, read `details.fontFamily`, `details.fontFile`, and `details.missingCodePoints`, then select and vendor a font whose real bytes cover them. For `font-coverage-unverified`, replace system or remote fallback with a project-local `@font-face`. Never infer support from a font's name.\n\nDo not suppress diagnostics, remove preconditions, or edit files behind VidCom.\n\nExample: `validate_project` → `get_project_context` → `read_composition` → targeted edit → `validate_project` → `start_snapshot`.\n",
    "contentHash": "sha256:74aa87ef09f4c198ad72db4d9033e0a9fbafc7498e865c5e8947fa0a0ce4f8d0"
  },
  "skills/vidcom/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom\"\n  short_description: \"Route video production work through VidCom MCP\"\n  default_prompt: \"Use VidCom MCP to build a value-first story with meaningful multi-phase motion in every story scene, then validate, preview, and render it safely.\"\n",
    "contentHash": "sha256:32f40ef0fdee7f91683de6a7131de9a5df606dbd54e649c7e513752dcdf2461e"
  },
  "skills/vidcom-project/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Project\"\n  short_description: \"Create and orient VidCom projects\"\n  default_prompt: \"Create or orient this VidCom project through MCP.\"\n",
    "contentHash": "sha256:d76342419cdef2921bdc5da4fee23c8724ace903836432f99df2297169d10500"
  },
  "skills/vidcom-scene/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Scene\"\n  short_description: \"Create and edit VidCom scenes\"\n  default_prompt: \"Build these VidCom scenes as value-first story beats, then route each beat through meaningful motion choreography.\"\n",
    "contentHash": "sha256:4983db3338605e25fad1c6bc6d8401a6090e10e7408e38c85b947b328655bd51"
  },
  "skills/vidcom-look/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Look\"\n  short_description: \"Adjust VidCom look and audio bed\"\n  default_prompt: \"Honor explicit colors, or choose a standardized palette, then adjust this video look through VidCom preview settings.\"\n",
    "contentHash": "sha256:b930c7b5c3ac1ffe6ddc54a3da83bc461e6143a2df86310017b59ac9e4761738"
  },
  "skills/vidcom-motion/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Motion\"\n  short_description: \"Choreograph story motion for VidCom scenes\"\n  default_prompt: \"Choreograph this story beat with setup, development, payoff, and hold; do not use fade-only motion.\"\n",
    "contentHash": "sha256:23fffdfd597ad026465e48ce4b8935d3a153a55cc59b24122607e6ef7dc7d584"
  },
  "skills/vidcom-narration/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Narration\"\n  short_description: \"Author and generate VidCom narration\"\n  default_prompt: \"Author and generate narration through VidCom MCP.\"\n",
    "contentHash": "sha256:7f18552e0e2676fc138372987b2a1e40b30c0535f151d8d433c243f6bb543597"
  },
  "skills/vidcom-render/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Render\"\n  short_description: \"Preview and render VidCom projects\"\n  default_prompt: \"Reject shallow fade-only scenes, verify each story beat's motion payoff, then preview and render through VidCom MCP.\"\n",
    "contentHash": "sha256:3e9b5731b8f8b9c15d465ed2cf545d6f1c34959af57e28ed72532ee5852ec6f3"
  },
  "skills/vidcom-fix/agents/openai.yaml": {
    "content": "# x-vidcom-agent-kit: 9\ninterface:\n  display_name: \"VidCom Fix\"\n  short_description: \"Diagnose and fix VidCom projects\"\n  default_prompt: \"Validate and fix this VidCom project through MCP.\"\n",
    "contentHash": "sha256:76788d1b52b66fb079ff145efd7ca885df6479b9438c0a559e2ec5bace3fad64"
  },
  "prompts/create-video.md": {
    "content": "<!-- x-vidcom-agent-kit: 9 -->\nCreate or continue a story-driven VidCom video for {{brief}}. Bootstrap the active host's VidCom agent kit and read its router first. Mark the project `vidcomAgentKitVersion: 9`, then propose a value-first spine and per-beat role, primary pattern, seam token, and motion map before editing. Keep generated story scenes at 6–10 seconds, distribute setup/development/payoff across first/middle/final thirds, use at least three primary patterns in every rolling four, and generate current narration over at least 75% of story time. A fade-only treatment, gentle rise/drop, repeated opacity-plus-translate entrances, root loops, and `data-no-timeline` are never bypasses. Honor explicit user or brand colors; otherwise choose and apply a standardized palette from `list_color_palettes`. Search for mood-appropriate open BGM first, verify its source and attribution, and install the exact result; use an offline built-in bed if providers are unavailable. Omit music only when silence was requested or editorially necessary. Validate every edit, inspect every payoff frame and the contact sheet, then render only after validation and motion review succeed.\n",
    "contentHash": "sha256:4c0af6d132ce77564f47c5003d8e73afb3f5843ea560c08a0560a446db058395"
  },
  "CLAUDE.md": {
    "content": "<!-- x-vidcom-agent-kit: 9 -->\n# VidCom workspace instructions\n\n## Skills — use these first\n\nStart with `/vidcom`. It routes the request to one focused skill:\n\n| Intent | Skill |\n| --- | --- |\n| Create, adopt, or orient a project | `/vidcom-project` |\n| Add or change scenes, timing, or text | `/vidcom-scene` |\n| Animate a scene, add 3D, particles, or Lottie | `/vidcom-motion` |\n| Change tone, palette, subtitles, or BGM | `/vidcom-look` |\n| Author narration or generate TTS | `/vidcom-narration` |\n| Snapshot, render, or inspect a job | `/vidcom-render` |\n| Diagnose and repair a project | `/vidcom-fix` |\n\n## Standard workflow\n\n1. BOOTSTRAP by attempting `install_agent_kit` once for the active host, then read the installed main or `.vidcom.md` auxiliary instructions and `/vidcom` before choosing a workflow. Do not block on `manual_merge` or loop until ready.\n2. DISCOVER the server protocol, capabilities, and tools.\n3. ORIENT with `list_projects` and `get_project_context`. Never guess IDs or timing.\n4. STORYBOARD before editing. State `This video tells [audience] that [message]`, then give every beat a narrative role, viewer experience, primary story pattern, meaningful visual change, setup/development/payoff, and a concrete seam token. The value claim lands by beat two. In every rolling four story scenes use at least three primary patterns.\n5. EDIT + CHOREOGRAPH with VidCom tools and the latest hash or revision precondition. Mark agent-authored projects with `vidcomAgentKitVersion: 9` in `hyperframes.json`. Every story scene is 6–10 seconds, needs setup, development, payoff, and hold across its first, middle, and final thirds, and routes through `/vidcom-motion` before rendering.\n6. STYLE from explicit user or brand colors when present. Otherwise call `list_color_palettes` with a controlled mood when possible; compare `chooseWhen`, `avoidWhen`, energy, temperature, harmony, and recommended uses, then apply the best id through `set_preview_settings`. Use `clean-slate` when no stronger match is justified.\n7. SCORE after duration is known: call `search_bgm` with the intended mood, verify the chosen source and attribution, then pass that exact result to `install_bgm`. If remote catalogues are unavailable or unsuitable, use `list_bgm_beds` as the offline fallback. Background music is the default; omit it only when the user asks for no music or silence is editorially required.\n8. VALIDATE with `validate_project`; resolve every error before completion.\n9. PREVIEW with `start_snapshot` and inspect real frames.\n10. NARRATE with `start_tts` for story scenes; generated, current narration must cover at least 75% of story time.\n11. RENDER with `start_render`; poll `get_job_status` with backoff.\n12. REPORT changes, final revision, and every remaining diagnostic.\n\nOn `write_conflict`, re-read and merge. Never remove a precondition. Destructive work requires explicit user confirmation.\n\n## Tool reference\n\n| Level | Tools |\n| --- | --- |\n| Read | `list_projects`, `list_catalog_items`, `get_project_context`, `list_scenes`, `read_composition`, `list_project_assets`, `get_narration_cues`, `list_tts_voices`, `list_color_palettes`, `search_bgm`, `list_bgm_beds`, `validate_project`, `get_job_status`, `get_render_output` |\n| Write | `create_project`, `adopt_project`, `rename_project`, `create_scene`, `set_scene_timing`, `set_element_position`, `set_text`, `save_file`, `set_preview_settings`, `replace_narration_cues`, `patch_narration_cue`, `install_motion_library`, `reorder_scenes`, `move_scenes`, `generate_captions`, `mount_asset`, `install_catalog_item`, `install_bgm`, `import_bgm`, `record_bgm_license` |\n| Job | `start_snapshot`, `start_tts`, `start_render`, `cancel_job` |\n| Destructive | `delete_file`, `delete_scene`, `delete_scenes`, `delete_project` |\n| Workspace | `install_agent_kit` |\n\nNo UI is required: `create_project` or `adopt_project` starts the work, `set_preview_settings` handles look and BGM, and `get_render_output` names the finished MP4 on disk. Only choosing the workspace directory and browsing the filesystem stay outside the tool surface.\n\n## Project structure\n\n- `index.html`: root composition and timeline mounts.\n- `compositions/`: one file per scene.\n- `assets/`: project-owned media; `assets/vendor/` holds pinned motion libraries.\n- `narration/`: narration cue sidecars and generated audio.\n- `preview-settings.json`: tone, subtitles, BGM, and preview settings.\n- `renders/`: VidCom-produced video artifacts.\n\n## Key rules\n\n- Give timed elements `class=\"clip\"`, `data-start`, `data-duration`, and `data-track-index`.\n- Keep GSAP timelines paused and register them at `window.__timelines[compositionId]`.\n- Add a motion library with `install_motion_library` and reference the vendored path it returns. Never load one from a CDN: the render stops being reproducible and resolves nothing offline.\n- For Vietnamese, Japanese, Korean, Chinese, or any other non-ASCII text, use a project-local `@font-face`. `validate_project` reads the actual font bytes and checks every authored Unicode code point; do not trust `system-ui`, a named machine font, or a remote font to render consistently.\n- Put every new scene in a separate sub-composition file mounted with `data-composition-src`.\n- On every story-scene root author `data-scene-role=\"story\"`, a content-specific `data-story-pattern`, and—after the first scene—`data-seam-kind=\"carry|transform|contrast\"` plus a concrete `data-seam-token`. Never use `fade`, `slide`, or `cards` as a primary pattern.\n- Story-driven video is the default. Build a value-first spine: hook in the viewer's language, tension or evidence that develops the claim, and a payoff that resolves it.\n- A scene is a story beat, not a slide. Its motion must reveal information, transform visual state, demonstrate cause/effect, move the camera to a new idea, or physically hand off to the next beat.\n- Give every non-trivial story scene multi-phase choreography: setup → development → payoff → hold. Compose 2-4 motion patterns and vary motion verbs, directions, and eases across scenes.\n- Distribute verified actions across the first, middle, and final thirds. Over every rolling four story scenes, keep at least three primary patterns; repeated adjacent patterns need a matching carry/transform token and a visible state handoff.\n- A lone fade, gentle rise/drop, or repeated `opacity + y` entrance is transition polish, not scene motion. Never use it as the primary choreography or repeat one entrance recipe across the video.\n- A tween after its scene clip ends never runs; move it or extend the clip.\n- Move a selected authored element or caption group with `set_element_position`; use the exact `data-hf-id` and current source hash, never a selector or client-supplied source path.\n- End on a held frame: the last scene runs 1-3s past its narration, every other scene about 0.3s. A video that stops on the final consonant feels cut off, not finished.\n- Change colors, tone, subtitle styling, and BGM through `set_preview_settings`, not composition source. Explicit user or brand colors always win; otherwise choose from the source-backed `list_color_palettes` guidance and apply `theme.paletteId`. An individual color override intentionally clears the preset id.\n- Background music is the default for every video unless the user requests no music or silence is editorially required. Add it after composition duration is known.\n- A file a person copied into the project never announces itself: run `list_project_assets` to find it, then attach a track with `set_preview_settings`.\n- Prefer `search_bgm` for real mood-matched instrumental music. Inspect `sourceUrl`, `license`, and `attribution`; pass only the chosen `{providerId, trackId}` to `install_bgm`, which revalidates that exact track and freezes its bytes, licence, and provenance locally. Never invent clearance from a search result.\n- Remote music is fail-soft: if providers are empty, rate-limited, or unsuitable, `list_bgm_beds` shows five built-in beds and `install_bgm` renders one at the project's own length. `import_bgm` records a supplied track's licence — state `unknown` rather than guessing, and use `record_bgm_license` only after somebody establishes the answer.\n- Keep rendering deterministic: no `Date.now()`, `Math.random()`, or network fetch.\n- Do not edit project files with host file tools or run HyperFrames CLI beside VidCom. If raw source editing is explicitly requested, use `save_file` with `expectedContentHash`.\n\n## Validate — always\n\nRun `validate_project` after every edit. Fix all `error` diagnostics before reporting completion. Then snapshot and inspect the affected frames.\n\n## Troubleshooting\n\n- `write_conflict`: refresh project context and retry from the new hash/revision.\n- `platform-mismatch`: align root width, height, and FPS with project platform settings.\n- `narration-overflow`: shorten/re-time the cue or extend the scene within duration limits.\n- `missing-asset`: restore or replace the referenced project-relative asset.\n- `story-motion-shallow`: redesign the beat with a meaningful state change across at least two statically visible phases; fade/position polish does not count.\n- `story-motion-unverified`: replace dynamic selectors with resolvable targets or add meaningful, statically verifiable GSAP choreography before render.\n- `story-metadata-missing`, `story-seam-missing`: add the v9 scene role, pattern, and concrete seam metadata.\n- `story-pattern-repeated`, `story-pattern-diversity`: redesign the sequence so a rolling four uses at least three primary patterns and any continuation has a visible matching handoff.\n- `story-scene-duration`: retime generated story scenes to 6–10 seconds.\n- `story-scene-static-too-long`: add current narration and continuing middle/final state change, or split the beat into shorter scenes.\n- `story-narration-sparse`: generate current narration until its clamped windows cover at least 75% of story time.\n- `remote-motion-library`: run `install_motion_library` and swap the CDN tag for the returned vendored tag.\n- `text-encoding-invalid`: re-save the referenced source as UTF-8 without replacement bytes.\n- `font-file-invalid`: restore or replace the referenced local OpenType font file.\n- `font-glyph-missing`: choose a local font whose `cmap` covers every code point listed in `details.missingCodePoints`.\n- `font-coverage-unverified`: vendor the selected font into the project and bind it with `@font-face`; machine or remote fallback is not portable evidence.\n- `lint:*`: apply the named HyperFrames rule, then validate again.\n",
    "contentHash": "sha256:0d140967ce202b56fa6ecd9e4278cffaabed3aa15984eea1602049ef86299e73"
  }
} as const;
