node_modules/
dist/
.vidcom-runtime-source-cache/
.vidcom-smoke-cache/
out/
coverage/
test-results/
.artifacts/
/release/
*.log
.env
.vidcom-tools/
.DS_Store
Thumbs.db
.temp-documents/
# The CLI end-to-end test stages a packed artifact here so it resolves the
# checkout's dependencies; it cleans up after itself unless a run is killed.
packages/cli/.artifact-*/
# Runtime state
config/*.json
!config/*.example.json

# Legacy app owns its detailed Python and generated-media ignores.

# Next.js
/.next/
/build
.vercel
next-env.d.ts
*.tsbuildinfo
.env*
!.env.example

# Python bytecode from the VieNeu sidecar (packages/adapter/sidecars/vieneu).
__pycache__/
*.py[cod]

# Generated by the build: the preview host page's only script.
public/preview-host.js
